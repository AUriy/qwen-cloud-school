# 🤖 Правила для Агента-Кодера

## Роли и Ветки
- **main**: 🔒 ЗАПРЕЩЕНО писать напрямую. Только через Pull Request после проверки человеком.
- **coder-<task>**: Твоя рабочая ветка. Создавай новую для каждой задачи (например, `coder-login-form`).
- **arch-guard**: Ветка архитектора (Qwen). Здесь лежат цели и отчеты. Не трогай файлы здесь, только читай.

## Алгоритм работы
1. Перед началом работы прочитай файл `--goal/goal-for-coder.md`.
2. Создай ветку: `git checkout -b coder-<название-задачи>`.
3. Пиши код, тестируй.
4. Сделай коммит и отправь в свою ветку.
5. Жди проверки архитектора.

## Важно
- НИКОГДА не делай `git push` в `main`.
EOF

cat > school/lesson-01.md <<'EOF'
# 📘 Урок 1-2: Безопасный периметр и Первый MVP

## 🏗 Архитектура веток (Протокол «Три Ветки»)

| Ветка | Роль | Кто пишет | Статус |
| :--- | :--- | :--- | :--- |
| **main** | Production (Боевая версия) | Только человек | 🔒 Защищена |
| **coder-*** | Песочница Кода | Локальный агент | ⚠️ Временная |
| **arch-guard** | Штаб Архитектора | Qwen | 🛡 Постоянная |

## 🛠 Шпаргалка Git
- `git commit --amend` — исправить последний коммит.
- `git checkout -b <имя>` — создать ветку.
- `git pull` — обновить текущую ветку с сервера.
EOF

# 5. Добавляем, коммитим и пушим в main
git add AGENTS.md school/lesson-01.md
git commit -m "Добавлена документация: AGENTS.md и school/lesson-01.md"
git push origin main
